import { TstDirective } from './tst.directive';

describe('TstDirective', () => {
  it('should create an instance', () => {
    const directive = new TstDirective();
    expect(directive).toBeTruthy();
  });
});
